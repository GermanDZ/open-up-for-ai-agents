- **Iteration 105** (2026-07-27): T-149 (standard) — **`docs/project-status.md` now has one field per question.** `**Status**` had two live readers wanting opposite things: `/openup-retrospective` reads it as the status of the iteration named in `**Iteration**`, while `on-task-request.py` reads it as "is a lane live?" — and it was written unconditionally from the active lane, so once T-146 guarded `**Iteration**` the pair could split (`Iteration: 104` sitting beside `Status: in-progress` from an unrelated quick lane). **That second reader is what decided the tie-break T-146 left open**: option (b) skip-on-quick was cheaper and would have made the header self-consistent, but it removes the hook's only live-lane signal without replacing it, so mid-quick-lane the hook would read a stale `completed` and **block a legitimate task-request** — trading a visible bug for an invisible one. Shipped (a): `**Status**` now sits behind the *same* truthy-`iteration` guard as `**Iteration**` (binding the pair into an invariant rather than a convention), and a new always-written `**Lane Status**` carries the lane's value; the hook reads it with a `**Status**` fallback, so un-migrated documents behave exactly as before. New `upsert_field()` inserts the field into headers that predate it — **`set_field()` deliberately stays replace-only**, since making it insert-when-missing would have been one word of diff and would have silently materialized every absent field (`Iteration Goal`, `Retrospective`, …) in every downstream document. **Tests verified to bite**: the 11 new writer-side tests were run against `git show HEAD:scripts/sync-status.py` — 9 fail there, 11 pass after (the 2 that pass both ways are deliberate no-regression guards). T-146's own `test_falsy_iteration_still_syncs_every_other_field` asserted the behavior this task changes and was re-pointed at `**Lane Status**` — flagged in `design.md`, because silently editing another lane's regression test is how a real regression gets laundered green. Full suite **863 passed**, 1 skipped, 20 subtests. **Found in passing**: `openup-claims.py claim --force` deletes the old claim before its remote-check refuses (and it refuses against the lane's *own* session), stranding the lane unleased — and the fence then reads *green* by falling back to frontmatter, so a lost lease looks like success. Recorded in `design.md` for triage.
