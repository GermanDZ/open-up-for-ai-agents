- **Iteration 88** (2026-07-25): T-128 (standard) — **unit-of-work fallback for the entropy analyzer + a human-authored baseline**. T-127's analyzer keyed every metric on a task id parsed from the commit subject, so it exited `3 — no telemetry` on any repo without OpenUP's convention — i.e. most repos, including every candidate for the comparison the decay thesis actually needs (agent-driven vs human-driven). New `--unit {task,commit,pr}` generalizes **only the graph's key**: `commit` makes a repo with no task ids measurable, `pr` groups by a trailing `(#N)` and drops untagged commits (mixing PR-sized and commit-sized rows would corrupt every median). The metric functions are untouched, and that was *proved* rather than asserted — the pre-change analyzer and the new one were run on this repo and their payloads compared object-for-object with `sources.unit` removed: **equal**. **No `auto` mode**: two reports on different units are not comparable (a task spans many commits), so the unit is explicit and printed in both the text header and `sources.unit`. Drift stays task-only — a commit has no declared surface, so it reports no data rather than inventing one. Regression caught while validating against real history: an **all-digit commit sha parses as a task ordinal**, and `bucket_by_index`'s `index is not None` filter then kept only those few commits — collapsing 672 units into 3 buckets of 1; the ordinal is now computed for the task unit alone. **Second baseline recorded**: `TallyFoxAI/ruby_llm` — a **fork** (601/672 commits by the upstream gem's maintainer, so it measures the upstream project, not TallyFox) — is **flat at 2 files/commit across nine months**, spanning the 3–6-month window, with a coupling profile shaped like this repo's (docs duplication, lockfile trios, module↔spec). Finding: `--unit commit` on *this* repo medians **0 files** (housekeeping commits touch only excluded paths), so the cross-repo comparison is a **trend-shape** comparison, never a level comparison. kaze-webapp re-verified unreachable by all three access paths. +6 tests; full suite 708 pass. Solo, standard, in-place branch.
