- **Iteration 111** (2026-07-28): T-160 (standard) — **two verification-honesty gaps closed.** *C2*: this repo has **two** project test directories and **no pack, skill or convention named a test command at all**, so "full suite" was free prose — T-155, T-157 and T-158 each quoted the `scripts/tests/` figure that way, omitting 114 tests in `tests/`, among them `tests/test_claims_heartbeat_reap.py`, the coverage most relevant to the very defect T-159 then had to fix. No regression resulted, but for three lanes the claim was broader than the command behind it. New `scripts/run-tests.sh` runs every project test directory, prints a per-directory summary, and never masks a failure (`test_does_not_mask_failures` pins the absence of `|| true` and the `exit "$overall"`, since a runner that returns 0 over a red directory is worse than none). Directories are **enumerated, not discovered** — `venv/` and `.claude/worktrees/` both contain vendored `test_*.py`, and a denylist rots invisibly, which is the same failure shape being fixed — so a guard test fails when a new project test directory is uncovered. **Bite-checked for real, not in a fixture**: adding `integration_tests/test_probe.py` to the repo made the guard fail naming that directory; removing it returned green. Rejected a `pytest.ini`/rootdir change, which would have silently altered every existing bare `pytest` invocation here and downstream. *C4*: T-152 made every measure name its read-back environment and that **worked** — but **naming is not access**. T-147 and T-155 both correctly named `kaze-webapp` and both still returned `can't tell`, three of nine verdicts lost in a single cycle. Criterion 12 is therefore **extended, not replaced**: when the environment is **not this repo**, the measure must also name a **reader** and cadence, and state **what an unreadable number means** — explicitly not "the change failed", since T-155's own note said a `0` there would mean "not delivered". The new elements fire **only** for non-local environments, so the common case gains no ceremony; both rubric copies edited identically, template and step 1b updated, mirrors re-rendered. **Discrimination checked rather than assumed**: across 122 archived specs, 112 state no environment (pre-T-152 or `n/a`), **7 name "this repo"** (correctly unaffected), and **3 are non-local — of which 0 name a reader**, so the criterion distinguishes and is forward-looking rather than retroactive debt. **Found while measuring: three at-risk measures, not two** — **T-139** also names a non-local environment, with a read-back due **2026-10-25** whose result decides whether T-156 is retired, making it the highest-consequence of the three; flagged for the next step 4b rather than rewriting an archived spec. Full suite via the new runner, stated per directory as the convention now requires: `scripts/tests/` **899 passed**, 1 skipped, 20 subtests; `tests/` **118 passed**.
