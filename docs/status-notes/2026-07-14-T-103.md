**Iteration 72** (2026-07-14): T-103 — **delete the hardcoded bootstrap + retire the per-cycle LLM navigator** — the final P1 task, realizing the program's falsifiable measure. Since **T-101** fires `plan-iteration` on a fresh project deterministically from the process map, the per-cycle navigator (T-096) + hardcoded Inception bootstrap (T-097/T-098) became **dead code on the fresh path** — now **deleted**: `scripts/openup_agent/navigator.py` removed entirely (the decision-file flow, `_bootstrap_step`, brief-specific scaffold, `run_navigator`), with its test file + process-manifest entry; `cycle.py` loses the `navigator` import, `_run_navigator`, the `navigate` param + the `_navigator` seam — its `noop` branch now just prints `OPENUP-NEXT: DONE — <reason>` (no navigate, no hardcoded hint); `--no-navigate` removed from the `openup-agent.py cycle` CLI. **The LLM is now used only to author artifacts, never to navigate per cycle** — the flaky per-cycle-LLM failure mode that produced the four live bugs (T-096→T-099) is deleted, not carried. Docs (`reference-driver.md`, `script-cli-reference.md`) replace the navigator sections with the deterministic-navigation note. Net-negative code. Tests: deleted `test_openup_agent_navigator.py` + the `NavigatorDispatchTest`; reverted the `navigate=False` switches (added in T-096) to plain DONE assertions; the fresh-project hint test became a plain-DONE test. Verified: `openup_agent` imports with no navigator; `cycle --help` has no `--no-navigate`; a fresh project → `resolve` = `plan-iteration` (`plan-fresh`) with navigator not shipped; **547 suite pass** (1 pre-existing stale `test_openup_state` schema-2-vs-1 failure, outside this lane's touches — the last chronic one; T-101 already fixed `test_promote_roadmap_task`). spec-scenarios, check-docs, `process-map validate`, fence (`--base harness-optional`) green. Solo, standard, worktree, on harness-optional. **This closes the P1 "Deterministic process-map navigation" program (T-100→T-101/T-102→T-103):** navigation is a deterministic walk of the process map; per-activity human inputs are declared as data and scaffolded; authoring activities run their procedure directly; the per-cycle LLM navigator is gone. Live-model program acceptance (zero per-cycle navigator calls — now structurally guaranteed — at ≤ prior token cost) is the T-080 owner batch. **P2 (LLM compile step for a customized process) remains deferred** (the T-100 schema is forward-compatible for it).
