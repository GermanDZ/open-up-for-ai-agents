- **Iteration 104** (2026-07-27): T-147 (standard) — **the write-fence no longer accuses a lane of writing files it cannot avoid writing**. `.claude/memory/bypass-log.md` (appended by three hooks) and `.claude/memory/iteration-learnings.md` (`/openup-complete-task` step 6, mandatory on every track) are written on every lane's behalf, but neither was in `openup-fence.py`'s lane-owned allowlist — so every lane in a project that **tracks** `.claude/` had to hand-declare both in its plan frontmatter `touches` just to get past the fence. **Premise measured downstream before any code** (action item B2's rule): in kaze-webapp both files are tracked and actively appended, its fence is the identical 3-entry constant, and **8 of 37 archived lanes carry the workaround** — the defect is structurally invisible here because this repo gitignores `/.claude/*`, so 'it doesn't reproduce locally' was never evidence against it. Supporting signal found while measuring: `on-stop.py` **already** lists `bypass-log.md` in its `EXEMPT_DIRTY_PREFIXES`, so this closes an internal inconsistency rather than inventing a policy. Named as **two files, not the `.claude/memory/` prefix** — a prefix would silently exempt whatever else a consumer keeps there — and that scope is asserted by a test in the *widening* direction, not just the removal one. **The doc edit changed under scrutiny**: the obvious move was to add both to `parallel-lanes.md`'s class-1 (lane-owned) row beside `docs/agent-logs/`, which would have been wrong — class 1 means *sharded, one writer per file*, while these are a single shared file every lane appends to. They are **class 2**, the class the doc says to avoid, re-populated involuntarily because hooks write them to fixed paths in the consumer's checkout. Exempting them at the fence removes the false `OUT OF LANE` but does **not** make them conflict-free; that residual is filed as **T-155** rather than bundled. Bite-checked both directions: removing the entries fails exactly the 2 new positive tests and **no pre-existing test** (proving a pure widening); widening to a prefix fails 2 different tests. +4 tests (845 collected on main → 849).
