- **Iteration 98** (2026-07-27): T-142 + T-143 (standard, one PR) — **the retro-cadence gate stops silently degrading**. Two independent mechanisms both broke it, so both ship together. *T-142 (when):* the increment was prose in exactly one skill (`/openup-complete-task` §7a); `/openup-quick-task` never mentioned `retro`, so every quick lane completed without advancing the cadence (T-133, T-137). Moved into `openup-state.py archive` — the one teardown step every completion path already runs — so it advances by construction, not by a model remembering a step; §7a is now a note explaining why there is nothing to run there (a second call would double-count). `--no-retro` opts out; a failed archive (exit 3) never advances. *T-143 (where):* `retro.json` lived next to `state.json` in the per-worktree, gitignored `.openup/`, so under this repo's own worktree-per-lane model every fresh lane restarted at `0` — re-confirmed live while working this lane, the worktree read `0` while main held `4`. Moved to `<git-common-dir>/openup/`, beside the claims dir: shared by every worktree, never merge-managed (which also kills the downstream overwrite-not-sum lost update). Read-forward, non-destructive migration; `--retro-dir` > `--state-dir` > shared > repo-local, so tests keep their isolation and a non-git checkout fails open. Also released an abandoned T-075 claim (merged 2026-07-13, worktree gone) whose repo-wide surface was blocking new claims. +10 tests; full suite 891 green.
