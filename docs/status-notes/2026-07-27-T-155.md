- **Iteration 106** (2026-07-27): T-155 (standard) — **`merge=union` for the two involuntarily-shared `.claude/memory/` files — and the delivery path that actually reaches the repos where they collide.** T-147 exempted these from the write-fence but left the merge question open, offering union / shard / accept. **Checked the premise downstream, read-only, before drafting** (action item B2): of the two consumers, `cqecho-app` tracks neither file and has no `.gitattributes` at all, while `kaze-webapp` tracks both — and there, `bypass-log.md` had **3 two-sided merges out of 3** (each a *local* `merge origin/main into <branch>` — exactly what a merge driver runs for) while `iteration-learnings.md` had **0**. So: one affected repo, one affected file, and a real-but-narrow problem. Chose **union over sharding**: unlike `agent-runs.jsonl` (consumed by tooling that can assemble a view), these two are read **directly at a fixed path** by an agent at session start, so sharding needs a consolidation step nothing runs, in the consumer's checkout, with the writers living downstream. **The actual finding was delivery, not the attribute**: `bootstrap-project.sh` copies `.gitattributes` only at *first* install and no updater touched it — so shipping the entry alone would have reached new projects only, and **none of the repos where the collision happens**. Hence `migrate_gitattributes_merge_union()` in `scripts/lib/migrate-data.sh`, called from `sync-from-framework.sh`, which appends (never overwrites), matches on the **path** not the whole line (so a consumer's own driver choice survives), creates the file when absent, and honors `--dry-run`. **Spec corrected mid-lane** (fix-spec-first): the first draft put the patch inline in the sync script, contradicting that file's own stated convention — *"logic lives in scripts/lib/migrate-data.sh so it is unit-testable"* — so `touches`/Structure/Operations moved before the code did. Honesty invariant carried into four places: this is a **mitigation**, since GitHub does not run merge drivers server-side and PR conflicts on these files remain. Full suite **873 passed**, 1 skipped, 20 subtests; the bootstrap assertion verified to bite (stripping the two lines failed exactly it). **Measure has a stated precondition**: it reads back in `kaze-webapp`, and until that repo runs its own updater a `0` means "not delivered", not "fixed".
