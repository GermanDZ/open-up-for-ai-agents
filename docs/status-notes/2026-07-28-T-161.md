- **Iteration 112** (2026-07-28): T-161 (standard) — **the loop's stop condition was broken exactly when the queue empties, and the first diagnosis of it was wrong.** `.openup/state.json` exists **only while a lane is in flight**, and `compute_status` fed `resolve_phase` from live state alone, falling straight through to `inception` — never consulting `docs/project-status.md`'s `**Phase**:`, the durable record `sync-status.py` writes at every completion. So **any** project with a drained roadmap and no active lane derived `inception`, which bypassed `openup-board.py`'s *deliberate* exclusion of construction/transition from the plan-fresh path and routed a mature repo into **authoring a fresh Vision, use cases and risk list**. Under an unattended `/loop` that is real damage before anyone looks. **The wrong diagnosis is the notable part**: this was first reported as `resolve` conflating "no promotable entries" with "no roadmap", pointing at the precedence — but reading the code refuted it, because the plan-fresh branch is gated on `phase in _AUTHORING_PHASES` and its own comment says *"Construction/transition are excluded, so their drained-roadmap case still falls through to noop."* The precedence was **already correct**; acting on the first reading would have produced a larger change in the wrong file and broken the fresh-project bootstrap that branch exists to serve. So **`scripts/openup-board.py` is untouched** — `git diff --stat` over it is empty — and requirement 7 verifies the corrected behaviour arrives *through* it. New tier: `milestone records → live state → project-status → inception`, with **live state still winning**, because mid-lane the state carries the phase actually being worked while the view still shows the previous completion (a test pins that direction, since it is the one the fix must not move). A hint that **fails validation** reports `state-fallback`, not the tier it came from — labelling it otherwise would credit a value that was discarded; `resolve_phase` was restructured so validation and attribution happen in one place. **Verified live on `main`, not only in fixtures**: swapping only the lifecycle script, `resolve` went from `plan-iteration`/`inception`/"no roadmap yet" to **`noop`/`construction`/"roadmap exhausted — no promotable pending task"** — exactly what `/openup-next`'s own sentinel contract prescribes; main was restored immediately. **Trigger is general, not deferral-specific**: T-073 and T-156 being deferred merely drained the roadmap and made a latent, permanent hazard visible — the same misfire awaits any repo whose backlog is fully delivered. Bite check: **3 of 8** new tests fail pre-fix; the other 5 pass both ways *by design*, being the no-regression guards for live-state-wins, fresh-default (×2), invalid-discarded and records-win. Suite via the runner, per directory: `scripts/tests/` **899 passed** (unchanged — no test there touched), 1 skipped, 20 subtests; `tests/` **118 → 126**.
