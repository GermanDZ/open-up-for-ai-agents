- **Iteration 87** (2026-07-25): T-127 (standard) — **report-only codebase-entropy measurement (M1) + first baseline (M3)**. New `scripts/openup-entropy.py`: deterministic, stdlib-only analyzer over telemetry the process already emits — declared `touches`, run-log shards, and `git log --numstat` joined on `[T-NNN]` subjects — reporting per-task cost (bucketed by task-index window and month), declared-vs-actual drift (coverage/precision/Jaccard), and co-change coupling (support/Jaccard/lift) over the declared and actual graphs independently. **Report-only by design**: no gate, no threshold, no write path — gates get justified by evidence, and this produces the evidence. Two measurement traps found and handled: (1) a **shallow clone** attributes the whole tree to the boundary commit (34/126 tasks visible, T-056 showing 2560 files) — `--unshallow` first; (2) declared `touches` carry **directory prefixes and inline YAML comments**, so `seg_prefix_collide` is imported from `openup-claims.py` rather than reimplemented — string equality understates agreement 13x (Jaccard 0.06 vs 0.81), in the direction that would falsely confirm decay. **First baseline: no decay signal** — actual files/task falls 7→2 across four index windows, module spread flat at 2, duration falls 14.5→9.5 min, while declared touches rise 2.5→5.5 (lanes declare more, change less). Drift median coverage 1.0 / precision 1.0 in the fence era. Coupling's top pairs are module↔its-test (Jaccard 1.0) and doc↔code created by fix-spec-first — healthy, and a warning that a naive D1 threshold would fire on them. Verdict recorded in `docs/explorations/2026-07-25-maintainability-baselines.md`: **D1 and R1 are not licensed** by this data; P1/P3 stand on their own merits. kaze-webapp + FacturaSimple baselines **blocked** on session repo scope (cross-owner `add_repo` refused), carried as an unticked Operations box. +23 tests; full suite 702 pass. Solo, standard, in-place branch.
