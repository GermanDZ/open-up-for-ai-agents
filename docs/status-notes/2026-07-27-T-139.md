- **Iteration 107** (2026-07-27): T-139 (standard) — **customized process sources: a project-owned override path, documented and pinned.** The last of T-107's three-way split. **Checked the premise before drafting** (action item B2, T-137 precedent) and **two of the four acceptance bullets were already satisfied**: `build-task-library.py --repo-root` has accepted a project-local input root since T-105, and a project-local library already overrode the framework's — `bootstrap-project.sh:145` copies the whole `docs-eng-process/` tree into every project, `_TASK_CANDIDATES[0]` is that path, and a scratch project's 1-def library fully shadowed the framework's 9 (`✓ valid — 1 task def(s)`). `sync-from-framework.sh:589` deliberately never overwrites that tree, so the customization already survived updates. What was missing was a **project-owned place** to put it, **any documentation** (`project-config.md` had zero mentions of task-library or process-map), and **any test**. **Scope decided by the owner before code**: document-and-make-honest, *not* the compiler emitter T-107 R4 implies — `build-task-library.py` writes no YAML for anyone (the framework's own library is hand-assembled and human-reviewed), and Stage-1 extraction parses only UMA-shaped docs, so the emitter's *input* premise is unverified; it ships as a follow-up with that premise named, the same disposition T-137 took. Delivered: `docs/process/{process-map,task-library}.yaml` prepended to both candidate tuples as the project-owned first match; both tuples re-commented per entry (the old `scripts/*.yaml` comment claimed "shipped-into-a-project fallback" — **nothing in the repo ships it**; kept as an escape hatch, honestly labelled); a `project-config.md` § "Customized process sources" covering resolution order, replace-not-merge, the `source: driver` rule for hand-authored defs, and an explicit statement that no library-generating compiler exists; a `reference-driver.md` pointer. **The compiler was not touched** — it inherits resolution through `_pm.load_tasks`, verified rather than assumed. **Test order followed the safeguard, not the Operations list**: R2's four no-override regression tests were written and passing against *unmodified* code before the tuples changed, so they guard the additive claim instead of describing the new behavior. 8 new tests, full suite green. **Measure has a stated precondition**: 0→≥1 consumer repo carrying `docs/process/` within 90 days (read-back 2026-10-25) across the four sibling repos, all verified reachable from this machine when the spec was written — and a `0` is a real result that should **retire** the emitter follow-up, since T-107's P2 premise ("a project wants to override the task library") has never been validated by an actual ask.
