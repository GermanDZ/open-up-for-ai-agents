- **Iteration 110** (2026-07-28): T-159 (standard) — **two silent failures, both fixed at the source.** *C1*: `openup-claims.py claim` wrote no `last_heartbeat`, and `cmd_reap` skips heartbeat-less claims **by design** (a backward-compat invariant for legacy files already on disk), so every claim created by bare `claim` was **permanently un-reapable** — and bare `claim` is the documented mid-lane re-claim recovery, which produced two such claims on 2026-07-27 alone. The iteration-109 retrospective's first instinct (an age-based `reap` fallback, recorded in `risk-list.md` R5) was **replaced by measuring the premise**: claims are *born* without the field, so stamping it at creation closes the hole and leaves `reap`'s invariant alone — and requirement 4 asserts that invariant in the direction the fix must **not** move, so a later simplification cannot quietly turn this into a reap change. One clock read serves both `claimed_at` and `last_heartbeat`; two timestamps microseconds apart would invite a reader to find meaning in the gap. *C3*: `sync-status.py` printed `Synced roadmap + project-status for <task>` even when **no roadmap entry matched at all** (observed live in T-158, where the roadmap never mentioned the task). New `roadmap_has_entry()` composes the two matchers `update_roadmap` already uses — so there is no second implementation to drift — and is called **before** the write, since afterwards the two cases are indistinguishable; `main()` now qualifies stdout and warns on stderr naming the task, the file, and the fix. **Exit stays 0 on purpose**: `/openup-complete-task`, `/openup-quick-task` and `openup_agent/cycle.py` all treat a non-zero sync as fatal, so a stricter code would convert a *reporting* bug into a *completion outage*. **Design corrected mid-lane** (fix-spec-first): the match flag was first specified as a third element on `update_roadmap()`'s return, but five pre-existing tests unpack it as a 2-tuple, so the spec moved to a predicate **before** any code was written; a new test now pins the 2-tuple so the rejected design cannot return silently. **A vacuous pass was caught and removed** — one new test back-dated `last_heartbeat` by assignment, introducing the very key whose absence *is* the defect, so it passed against the unfixed code; it now asserts the field exists first. Bite check: **3 of 4** C1 and **5 of 7** C3 tests fail pre-fix, the remainder being deliberate no-regression pins. **Both suites reported separately**, per this lane's own C2 sibling: `tests/` **114 → 118**, `scripts/tests/` **884 → 891**, no assertion edited in either.
