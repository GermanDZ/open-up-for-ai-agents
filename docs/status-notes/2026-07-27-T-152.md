- **Iteration 101** (2026-07-27): T-152 (standard) — **a success measure must now name the environment its read-back will happen in**. T-052's measure was not vague: it named a concrete instrument (`.claude/memory/bypass-log.md`) and a concrete date, for two downstream repos. At read-back neither repo had the file, so `grep -c` → 0 could not be distinguished from "not logging" — dead measure, after sitting as a carried action item for 88 iterations. **The gap was one of standpoint, not rigour**: rubric criterion 12 and `/openup-complete-task` step 1b both evaluated "does the instrument exist?" from wherever the completing agent stood — the framework repo — so a measure about *downstream* behaviour passed while being unreadable where the number had to come from. Criterion 12 gains the **read-back environment** as a required element plus two new gaps (no environment named; instrument exists somewhere other than the read-back environment); step 1b now checks existence **in that environment** and states explicitly that "it exists in this repo" does not satisfy a downstream expectation; the authoring template gains a `Read-back environment:` line. **No validator script** — criterion 12 is graded prose, and the real question (*does it exist there?*) is not parseable; a name-matcher would pass any phrasing while answering nothing. Discrimination checked rather than assumed: across archived specs carrying a real measure, **48/48 lack a read-back environment and 0/48 have one**, so the criterion distinguishes rather than passing by construction — and that is a forward-looking rule, not retroactive debt, since the element did not exist when those were written. Both rubric copies identical, mirrors re-rendered, sync + check-docs clean.
