- **Iteration 102** (2026-07-27): T-153 (standard) — **the install path is now actually run by a test**. Verified before building (T-151's lesson) that two of the three sub-items in the iteration-86 action item already had real coverage: `sync-from-framework` detection (`test_sync_from_framework_detection.py`, 3 tests over real consumer fixtures) and the tracked bypass-log dirty-stop (`test_t006_hooks.py`). **The genuine gap was none of the three: no test ran the installer at all.** Every existing test starts from a hand-built fixture asserting what its author *believed* `bootstrap-project.sh` produces — which is exactly how T-110 (consumer shipped with no self-updater) and T-150 (settings referencing hook scripts not yet on disk, locking Bash and Write together) both reached downstream repos. New `scripts/tests/test_consumer_smoke.py` runs the real installer into `tmp_path` and asserts four **usability** properties: the OpenUP CLIs are shipped and runnable, the self-updater is shipped (T-110), **every hook command is existence-guarded** (T-150 from the consumer side — a consumer is the likeliest place for settings and scripts to be out of step), and the consumer does not carry the framework-exclusive marker so it cannot be misdetected as its own upstream (T-126). **Deliberately not an inventory test** — asserting every `process-manifest.txt` entry would restate the manifest and fail on every legitimate addition, which is how a smoke check becomes noise and then gets deleted. **Verified to bite rather than assumed**: stripping the T-150 guard from the shipped template failed exactly `test_every_consumer_hook_command_is_guarded` and nothing else; restoring returned all 7 green. Module-scoped fixture keeps the whole file at ~3.5s. Full suite **946 green** (1 skipped).
