- **Iteration 100** (2026-07-27): T-151 (standard) — **two of iteration 98's own action items were misdiagnoses; both retracted with evidence, and the two decisions they were blocking are now recorded**. Measuring before building overturned the whole task. **A2 ("completion double-increments the retro counter")** is *obsolete*: T-142 (commit `177ee42`) had already removed the duplicate and shipped the regression tests, merging as PR #93 **mid-session** — the lane that observed the double-increment was executing a `.claude/skills/` mirror rendered before that merge, so the observation was accurate and the diagnosis wrong. **A3 ("the two `retro.json` stores disagree; `reset` reaches only one")** is *wrong*: in an isolated fixture, after `reset` the authoritative store and `get` both read 0, and the legacy `.openup/retro.json` is a deliberate one-time migration seed ignored once the authoritative file exists (rewritten to 4, `get` still reported 100) — the original claim was inferred from two direct file reads, never from `get`. Both struck through **in place** with the disproving evidence rather than deleted, per T-141's rule that a wrong finding must stay auditable; A3 is labelled **WRONG**, not "done", because a plausible storage bug is exactly what gets re-derived from scratch otherwise. **No code changed** — counter, store, migration branch and single increment site all measured correct. What remained was real and is now done: carried decisions **9.1** (gate fires at `count >= 5` — semantics written down, no change) and **77.2** (hard block stays `full`-only, with an explicit revisit condition: iterations 87–98 *did* outrun the cadence on standard lanes, but that streak was counted by the very number proven untrustworthy, so a second occurrence is the clean evidence) recorded in `state-file.md`, which also gains the reusable trap: **a mid-session merge leaves an agent on a pre-sync skill mirror, so an apparent framework bug may be a stale copy** — same class as T-140 DD5 and T-150's root cause. 19 retro tests green.
