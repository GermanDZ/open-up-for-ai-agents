- **Iteration 103** (2026-07-27): T-148 — `openup-session.py begin` now auto-resolves `gates.plan_persisted` from the task's own `docs/changes/<id>/plan.md` on standard/full, so the manual `set-gate plan_persisted` recovery (applied on five consecutive tasks, and observed live downstream) is gone. Explicit `--plan` still wins, a missing spec is fail-open, and `quick` is skipped; a `plan_gate_autoresolved` run-log event makes the effect measurable. The start-iteration pack's step 6 no longer advertises the wrong `docs/plans/` path.
